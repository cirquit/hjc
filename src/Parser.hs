import Parser where

import BNF

-- eval :: Stm -> IO ()
-- eval (ComboundStm stm1 stm2) = do 
-- eval (PrintStm expList) = map print (evalExp expList)

-- evalBinOp :: BinOp -> (Int -> Int -> Int)
-- evalBinOp MINUS = (-)
